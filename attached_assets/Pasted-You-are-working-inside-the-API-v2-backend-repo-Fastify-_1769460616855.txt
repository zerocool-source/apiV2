You are working inside the API v2 backend repo (Fastify + Prisma + Postgres) that deploys to Render.

GOAL:
Implement incremental sync + pagination on core GET endpoints by adding:
- updatedSince (ISO timestamp)
- limit (default 50, max 200)
- cursor (id-based cursor pagination)
Return shape:
{ items: [...], nextCursor: "<id>" | null }

DO NOT create new /api/sync/* endpoints. We are upgrading the existing API routes.

ENDPOINTS TO UPDATE:
1) GET /api/properties
2) GET /api/technicians
3) GET /api/assignments

REQUIREMENTS (MUST):
A) Query params supported consistently across all 3 endpoints:
- updatedSince: optional ISO string; filter updatedAt > updatedSince
- limit: optional int; default 50; clamp to max 200
- cursor: optional string; use Prisma cursor pagination

B) Response format for each endpoint:
Return JSON object:
{
  "items": <existing array response>,
  "nextCursor": <string|null>
}
Where nextCursor is the id of the last item in the returned page if more results exist; else null.

C) Pagination logic:
- Use Prisma findMany with take = limit + 1
- If cursor provided: include cursor: { id: cursor } and skip: 1
- Determine hasMore by items.length > limit
- Slice to limit and compute nextCursor

D) Ordering:
Use stable ordering:
orderBy: [{ updatedAt: "asc" }, { id: "asc" }]
Use the model’s own updatedAt + id.

E) RBAC MUST remain correct:
- properties: keep supervisor/team isolation behavior exactly as currently implemented
- technicians: keep role logic exactly as implemented (supervisor sees only their team, etc.)
- assignments: keep existing RBAC, and keep the default behavior that excludes cancelled/canceled unless includeCanceled=true (if already implemented). The new updatedSince must apply on top of that.

F) updatedSince filter:
- properties: apply to Property.updatedAt
- technicians: apply to TechnicianProfile.updatedAt if possible; if it becomes too complex with Prisma ordering, then:
  - apply updatedSince to technicianProfile.updatedAt in the where clause
  - paginate using User.id cursor with stable order (User.updatedAt + id)
  - leave a TODO comment if you can’t perfectly order by profile.updatedAt without refactoring
- assignments: apply to Assignment.updatedAt

G) Utility helper:
Create a reusable helper in src/utils/pagination.ts:
- parseLimit(raw, default=50, max=200)
- parseUpdatedSince(raw) => Date | undefined (ignore invalid strings)

H) Swagger/OpenAPI:
Update route schemas so Swagger shows these query params on each updated endpoint:
- updatedSince (string)
- limit (integer)
- cursor (string)

I) Docs:
Update replit.md with:
- example curl for first page + next page using cursor
- example curl using updatedSince
for all three endpoints.

J) Tests:
If there are existing tests, update them to reflect the new response shape (items/nextCursor).
If no tests exist, add at least one lightweight test per endpoint OR add a small “manual test” section in replit.md with copy/paste curl commands.

DELIVERABLES:
- Code compiles
- Server runs
- /docs/json reflects the new query params
- Each endpoint returns {items,nextCursor}
- No RBAC regressions
- Commit changes with message: "Add updatedSince + cursor pagination to core GET endpoints"
