GOAL (Option B): Make API v2 (Fastify) the single source of truth for Tech Ops "Repairs Needed".
Mobile app + Admin app must BOTH read/write Tech Ops via Render API v2.

CURRENT PROBLEM:
- API v2 responds at /api/health but /api/tech-ops returns 404.
- Admin UI expects Tech Ops entries filtered by entryType=repairs_needed.
- We must implement /api/tech-ops in API v2 and point Admin+Mobile to it.

TASKS (DO THESE IN ORDER, DO NOT SKIP):
1) API v2: Add a Tech Ops data model/table based on BACKEND_API_BLUEPRINT.md techOpsEntries:
   - fields: id, entryType, technicianName, technicianId, propertyId, propertyName, propertyAddress,
     description, notes, priority, status, isRead, chemicals, quantity, issueType, photos, reviewedBy,
     reviewedAt, createdAt, updatedAt.
   - entryType must support at least: repairs_needed
   - priority supports: normal/urgent
   - status default: pending
2) API v2: Create a new route module techOpsRoutes and register it in src/app.ts:
   - await fastify.register(techOpsRoutes, { prefix: '/api/tech-ops' });
3) API v2: Implement endpoints:
   A) GET /api/tech-ops
      - supports optional filters: entryType, status, priority, propertyId, technicianName, startDate, endDate
      - returns array sorted newest-first
   B) POST /api/tech-ops
      - accepts JSON body with entryType="repairs_needed", description, propertyName, technicianName, priority
      - validates minimum requirements (description required unless audio supported later)
      - returns created entry
4) Deploy API v2 to Render and verify:
   - GET https://breakpoint-api-v2.onrender.com/api/tech-ops?entryType=repairs_needed returns 200
5) Admin app:
   - Update frontend API calls for Tech Ops to use VITE_API_URL (Render API v2), not relative "/api/tech-ops"
   - Ensure Repairs Needed tab fetches: `${VITE_API_URL}/api/tech-ops?entryType=repairs_needed`
6) Mobile app:
   - Set EXPO_PUBLIC_API_URL=https://breakpoint-api-v2.onrender.com
   - Set EXPO_PUBLIC_TECHOPS_URL=https://breakpoint-api-v2.onrender.com (or remove and fall back)
   - Ensure Repairs Needed submits POST to /api/tech-ops with entryType="repairs_needed"

OUTPUT REQUIRED:
- List of files changed in API v2 + Admin + Mobile
- Exact new endpoints + example request/response JSON
- Verification steps and expected status codes
